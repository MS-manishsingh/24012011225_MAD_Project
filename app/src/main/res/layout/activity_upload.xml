<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#0F172A">

    <!-- Title -->
    <TextView
        android:id="@+id/txtUploadTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Upload Speed"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:gravity="center"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="8dp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/txtUploadSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Test your internet upload speed"
        android:textSize="14sp"
        android:textColor="#A5B4FC"
        android:gravity="center"
        android:layout_marginBottom="30dp" />

    <!-- SIM Selection -->
    <TextView
        android:id="@+id/txtUploadSimTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select SIM"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="10dp" />

    <RadioGroup
        android:id="@+id/uploadSimGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <RadioButton
            android:id="@+id/uploadRadioSim1"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="SIM 1"
            android:textSize="16sp"
            android:textColor="#FFFFFF" />

        <RadioButton
            android:id="@+id/uploadRadioSim2"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="SIM 2"
            android:textSize="16sp"
            android:textColor="#FFFFFF" />

    </RadioGroup>

    <!-- Selected SIM -->
    <TextView
        android:id="@+id/txtUploadSelectedSim"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Selected SIM: SIM 1"
        android:textSize="15sp"
        android:textColor="#A5B4FC"
        android:gravity="center"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="30dp" />

    <!-- Loading Animation -->
    <ProgressBar
        android:id="@+id/progressUpload"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_gravity="center"
        android:indeterminate="true"
        android:visibility="gone"
        android:layout_marginBottom="20dp" />

    <!-- Upload Speed -->
    <TextView
        android:id="@+id/txtUploadSpeed"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="0.00 Mbps"
        android:textSize="38sp"
        android:textStyle="bold"
        android:textColor="#4ADE80"
        android:gravity="center"
        android:layout_marginTop="15dp"
        android:layout_marginBottom="10dp" />

    <!-- Status -->
    <TextView
        android:id="@+id/txtUploadStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Ready to test"
        android:textSize="15sp"
        android:textColor="#94A3B8"
        android:gravity="center"
        android:layout_marginBottom="30dp" />

    <!-- Start Button -->
    <Button
        android:id="@+id/btnStartUpload"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Start Upload Test"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginBottom="15dp" />

    <!-- Back Button -->
    <Button
        android:id="@+id/btnUploadBack"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Back"
        android:textSize="16sp" />

</LinearLayout>